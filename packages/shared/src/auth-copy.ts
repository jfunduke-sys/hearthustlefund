/** Shown next to password fields when users create an account (web + mobile). */
export const NEW_PASSWORD_REQUIREMENT_COPY =
  "Passwords must be at least 8 characters.";

/** Participant account creation — COPPA age screening (not parental consent). */
export const PARTICIPANT_AGE_ATTESTATION_CHECKBOX_COPY =
  "I confirm that I am at least 13 years old.";

export const PARTICIPANT_AGE_ATTESTATION_REQUIRED_ERROR =
  "You must confirm you are at least 13 years old to create an account.";
