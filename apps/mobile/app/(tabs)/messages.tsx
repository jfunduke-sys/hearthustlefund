import FundraisingContactsScreen from "../../components/fundraising-contacts";

/** Athlete tab: pick phone contacts, save for texting, send initial fundraising SMS. */
export default function SendMessagesScreen() {
  return <FundraisingContactsScreen variant="athlete" />;
}
