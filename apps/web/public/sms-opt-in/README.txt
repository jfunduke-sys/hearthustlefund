Add two iPhone screenshots for Twilio A2P review (optional but recommended):

  app-opt-in-unchecked.png  — Dashboard → Your Contact Info, checkbox OFF
  app-opt-in-checked.png    — same screen, checkbox ON, before Save

Commit and deploy with the website before resubmitting the Twilio campaign.
